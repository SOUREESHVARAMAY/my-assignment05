import React from "react";

const Header = () => {
  const headerStyle = {
    backgroundColor: "#282c34",
    color: "white",
    padding: "10px",
    textAlign: "center",
  };

  return <header style={headerStyle}>Weather App</header>;
};

export default Header;
